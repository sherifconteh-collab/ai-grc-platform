# Documentation review — PR #209

Performed live by a Claude Code session with direct PR/repo access (no
GitHub Action, no separate API key) — replaces the removed
`claude-doc-review.yml` workflow, which needed a manual GitHub App
install + `ANTHROPIC_API_KEY` before it could run at all.

Scope: this repo has no per-feature `docs/guides/` structure or automated
feature-change detector like the sibling ControlWeaver-Pro repo, so this
review checked the top-level docs (`README.md`, `PROJECT_STATUS.md`,
`RELEASE_NOTES.md`, `docs/*.md`) against the auth/RBAC/auditor-workspace/
AI-feature changes in this PR's most recent commits.

## Verified already correct — no change needed

- **Password policy**: `README.md` ("15-character minimum password policy
  with complexity rules") and `PROJECT_STATUS.md` ("15-character minimum
  password policy") both already state the real, current policy
  (`utils/passwordPolicy.js` `MIN_PASSWORD_LENGTH = 15`). No staleness
  here — unlike the sibling repo, where the equivalent guides said 12.
  This PR's auth fix (complexity now enforced on register/reset, not just
  invite acceptance) doesn't change the stated minimum length, so no edit
  needed.
- No doc in this repo makes any claim about the auditor-workspace public
  page's engagement-name display, or about "no AI provider configured"
  error behavior for the specific AI features this PR touched — so
  nothing here contradicts the fixes in this PR. This repo's docs are
  thinner on these specific topics than the sibling repo's `docs/guides/`
  set; not a regression, just less granular coverage to begin with.

## Not applicable to this repo

- This repo has no `TIER_COMPARISON.md` or tier-gated auditor-workspace
  claim to reconcile (ai-grc-platform never had premium-tier gating in
  the same form the sibling repo is cleaning up).
