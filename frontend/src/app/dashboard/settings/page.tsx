// @tier: community
import SettingsScreen from '@/components/settings/SettingsScreen';

// The bare route shows the reader's default section and rewrites the URL to it.
export default function SettingsPage() {
  return <SettingsScreen />;
}
